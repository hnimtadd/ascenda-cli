{
  "name": "ascenda-cli",
  "version": "1.0.0",
  "description": "commandline application which respond to find nearby offers for customers.",
  "exports": "./index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js  --bundle --outfile=./build/build.cjs --format=cjs --platform=node && pkg ./build/build.cjs -o ./build/cli"
  },
  "author": "Craftznake",
  "license": "MIT",
  "dependencies": {
    "@vercel/ncc": "^0.38.1",
    "esbuild": "^0.19.8",
    "pkg": "^5.8.1"
  }
}
